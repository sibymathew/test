language: python
python:
 - 2.7
install:
 - pip install -r requirements.txt
script:
 - uname -a
 - which python
 - python src/application.py&
env:
 - AWS_KEY_ID=SIBYMATHEW
commit_container: sibymath/siby_test_project
