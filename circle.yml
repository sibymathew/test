machine:
  python:
    version: 2.7.3
  services:
    - docker

dependencies:
  pre:
    - pip install requirements-txt
    - docker build -t siby/circletest:v1 .

test:
  post:
    - docker run -d -p 5000:5000 siby/circletest:v1; sleep 10
    - curl http://localhost:5000/1/ping
