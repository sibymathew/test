from ..models.account import Account 

class AccountSzr():

    # still thinking if we need a serializer if we are using models.
    # Planning on serializing using a funtion within the models.
    pass