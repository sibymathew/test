{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "supported_device",
  "description": "Details of the device/gateway supported by VRIoT engine",
  "type": "object",
  "properties": {
    "_id": {
      "description": "Unique mongodb id for a document. Auto generated",
      "type": "string",
      "index": {
        "$ref": "/indexes/_id"
      }
    },
    "device_classification": {
      "description": "classification of Device is a GATEWAY or DEVICE or ROUTER",
      "type": "string",
      "index": {
        "$ref": "/indexes/device_classification"
      }
    },
    "device_type": {
      "description": "Type of the device used inside VRIoT. LIGHT or SWITCH or LOCK or HVAC",
      "type": "string",
      "index": {
        "$ref": "/indexes/device_type"
      }
    },
    "device_command_type": {
      "description": "Type of the device to be sent in command. LIGHT or SWITCH or LOCK or HVAC",
      "type": "string",
      "index": {
        "$ref": "/indexes/device_command_type"
      }
    }
  },
  "required": [
    "device_classification",
    "device_type",
    "device_command_type"
  ],
  "indexes": {
    "_id": {
      "description": "Mongo DB default index on document",
      "type": "string"
    },
    "device_classification": {
      "description": "For faster search on device classification",
      "type": "string"
    },
    "device_type": {
      "description": "For faster search on device type",
      "type": "string"
    },
    "device_command_type": {
      "description": "For faster search on device command type",
      "type": "string"
    }
  }
}